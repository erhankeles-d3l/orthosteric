# Audit Reports

Versioned home for Stage 0 audit outputs (`SCI0-015`, the public comparative evidence audit), attached to the snapshot hash they were computed against.

Established by `ADR-0006` (A2). No content is filed here retroactively. `SCI0-015` has not started — this directory is empty until that objective produces its first report.
